# QuantAI gitleaks configuration
#
# Inherits all default gitleaks rules. No project allowlist — the
# `quantai_dev_2026` dev-fallback password was removed from all source
# files in 2026-04-28 and the allowlist block was deleted with it.
#
# Read automatically by both:
#   - the local pre-commit hook (gitleaks/gitleaks @ v8.18.4)
#   - the server-side GitHub Actions workflow (.github/workflows/security-scan.yml)

[extend]
useDefault = true
