# trading-system/strategy/requirements.txt
#
# Pin major versions. Update deliberately, never automatically.

# GCP
google-cloud-bigquery==3.25.0
google-cloud-pubsub==2.25.0
google-cloud-secret-manager==2.21.0
google-cloud-storage==2.18.2
google-auth==2.35.0

# Data & ML
pandas==2.2.3
numpy==1.26.4
# scikit-learn and scipy removed — not imported anywhere in strategy code

# Financial data
# ibapi is installed manually from IBKR TWS API download (not on PyPI)
yfinance==0.2.44        # Fallback data source for backtesting only

# Decimal precision
# Use Python's built-in decimal module — no extra package needed

# API / gRPC bridge
grpcio==1.67.1
grpcio-tools==1.67.1
protobuf==5.28.3

# Backtesting
# vectorbt is optional — heavy dependency, install separately if needed
# vectorbt==0.26.2

# PostgreSQL driver (used by fetcher.py, update_daily_pnl.py, seed_yfinance.py)
psycopg2-binary==2.9.10

# Utilities
python-dateutil==2.9.0
pytz==2024.2
# tqdm removed — progress bar not used in strategy code

# Development / testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
black==24.10.0
ruff==0.8.1
mypy==1.13.0
